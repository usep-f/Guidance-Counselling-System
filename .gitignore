# Dependencies
node_modules/

# Firebase Admin secret keys (NEVER commit)
admin-tools/*.json
admin-tools/serviceAccountKey.json
admin-tools/set-admin-claim.cjs
.firebase
chatbot_config.js

# Local env files (if you use them)
.env
.env.*
